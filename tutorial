Print parts, insert key and driver pins into cartridges, snap them into the shell, add the plug and shackle,
then screw the halves together. Apply tension with the wrench and lift pins using the pick until the plug rotates. 
Adjust pin heights to change difficulty.
